# 第二阶段清单（Phase 2）

> 目标读者：执行开发任务的 Agent
> 目标版本：v1 + v2 增强版
> 最终目标：实现完整的本地智能图片管理核心功能

## 前置条件

Phase 1 已完成：
- [x] Android 工程骨架（Compose + WebView）
- [x] H5 工程（Vue3 + Vant4）
- [x] JSBridge 通信机制
- [x] Debug/Release APK 打包

## 1. 第二阶段目标

### 核心功能（v1 完整版）

- [ ] SQLite 数据库层（Room + Entity + DAO）
- [ ] 图片扫描与索引（MediaStore）
- [ ] AI 结构化标注与落库
- [ ] FTS5 全文检索
- [ ] 看图查标签（库内图片）
- [ ] 看图查标签（外部临时图片）

### 增强功能（v2）

- [ ] 标签标准化工具（alias/canonical）
- [ ] 以图反查 v1（标签召回 + hash 粗排）
- [ ] 搜索结果解释与修正闭环

## 2. 环境依赖补充

### Android 新增依赖

```kotlin
// Room
implementation("androidx.room:room-runtime:2.6.1")
implementation("androidx.room:room-ktx:2.6.1")
ksp("androidx.room:room-compiler:2.6.1")

// OkHttp (AI API 调用)
implementation("com.squareup.okhttp3:okhttp:4.12.0")
implementation("com.squareup.okhttp3:logging-interceptor:4.12.0")

// 协程
implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")

// DataStore (配置存储)
implementation("androidx.datastore:datastore-preferences:1.1.1")
```

### 权限补充（AndroidManifest.xml）

```xml
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.INTERNET" />
```

## 3. 任务分解

### P1. 数据基础层

#### P1.1 Room 数据库配置

- [ ] 添加 Room 依赖到 `libs.versions.toml` 和 `app/build.gradle.kts`
- [ ] 创建 `AppDatabase.kt` 数据库类
- [ ] 配置数据库版本与迁移策略

完成标准：
- [ ] `./gradlew assembleDebug` 编译通过
- [ ] 数据库类可正常初始化

#### P1.2 Entity 定义

按方案.md第七章定义：

- [ ] `ImageEntity.kt` - 图片元数据表
- [ ] `ImageAiEntity.kt` - AI 标注结果表
- [ ] `TagEntity.kt` - 标签表
- [ ] `ImageTagEntity.kt` - 图片-标签关联表
- [ ] `TagAliasEntity.kt` - 标签别名表
- [ ] `ImageQueryCacheEntity.kt` - 临时查询缓存表
- [ ] `ImageFeatureEntity.kt` - 特征表（v2 使用）

完成标准：
- [ ] 所有 Entity 定义完成
- [ ] 包含正确的索引和外键约束

#### P1.3 DAO 接口

- [ ] `ImageDao.kt` - 图片 CRUD
- [ ] `ImageAiDao.kt` - AI 结果 CRUD
- [ ] `TagDao.kt` - 标签管理
- [ ] `ImageTagDao.kt` - 关联关系
- [ ] `SearchDao.kt` - FTS5 检索

完成标准：
- [ ] 所有基础 CRUD 方法实现
- [ ] 包含事务注解的批量操作方法

#### P1.4 FTS5 虚拟表

- [ ] 创建 `image_fts` 虚拟表
- [ ] 实现插入触发器（自动同步 search_tokens）
- [ ] 实现 BM25 排序查询

完成标准：
- [ ] FTS5 表创建成功
- [ ] 全文检索可返回按相关性排序的结果

---

### P2. 图片接入层

#### P2.1 MediaStore 扫描服务

- [ ] 创建 `ImageScanner.kt` 类
- [ ] 实现 `scanAllImages()` 全量扫描
- [ ] 实现 `scanNewImages(sinceTimestamp)` 增量扫描
- [ ] 处理 Android 13+ 权限适配

完成标准：
- [ ] 可扫描设备上所有图片
- [ ] 增量扫描返回新增图片列表

#### P2.2 图片预处理

- [ ] 创建 `ImagePreprocessor.kt` 类
- [ ] 实现 SHA256 计算（使用 MessageDigest）
- [ ] 实现图片压缩（目标 < 512KB）
- [ ] 实现 EXIF 旋转纠正

完成标准：
- [ ] 预处理后的图片可上传 AI API
- [ ] SHA256 值正确计算并存储

#### P2.3 入队管理

- [ ] 创建 `ProcessQueue.kt` 处理队列
- [ ] 实现图片入库逻辑
- [ ] 实现去重（基于 SHA256）
- [ ] 实现后台协程处理

完成标准：
- [ ] 新图片自动入队处理
- [ ] 队列持久化，App 重启后恢复

---

### P3. AI 分析层

#### P3.1 HTTP 客户端

- [ ] 创建 `AiHttpClient.kt` 单例
- [ ] 配置 OkHttp（超时、重试、日志）
- [ ] 实现 API Key 管理（从 BuildConfig 或 DataStore）

完成标准：
- [ ] HTTP 客户端可正常发起请求
- [ ] 错误处理完善

#### P3.2 AI API 调用

- [ ] 创建 `AiService.kt` 接口
- [ ] 实现 `analyzeImage(imageUri: Uri)` 方法
- [ ] 解析 AI 返回的 JSON（按方案.md第六章 Schema）

AI 返回结构：
```json
{
  "caption": "描述文本",
  "categories": ["真实照片/风景"],
  "tags": [{"name": "海边", "score": 0.95}],
  "search_tokens": ["海边", "沙滩"],
  "ocr": [],
  "safety": {"nsfw": false},
  "score": {"quality": 0.92}
}
```

完成标准：
- [ ] AI 调用成功并返回结构化数据
- [ ] 错误处理（超时、限流、失败）

#### P3.3 结构化落库

- [ ] 创建 `AiResultProcessor.kt` 处理类
- [ ] 实现 JSON 解析为 Entity
- [ ] 实现标签 canonical 化（查 alias 表）
- [ ] 实现多表事务写入

完成标准：
- [ ] AI 结果正确写入 image_ai、tag、image_tag 表
- [ ] FTS 索引自动更新

#### P3.4 版本追踪

- [ ] 记录 `ai_model`、`prompt_version`、`schema_version`
- [ ] 实现按版本查询/重跑能力

---

### P4. 搜索与检索层

#### P4.1 文本检索

- [ ] 创建 `SearchService.kt`
- [ ] 实现关键词 canonical 化
- [ ] 实现 FTS5 MATCH 查询
- [ ] 实现 BM25 排序

完成标准：
- [ ] 输入关键词可返回相关图片列表
- [ ] 支持多关键词 AND 查询

#### P4.2 看图查标签（库内）

- [ ] 实现 `getImageTags(imageId)` 方法
- [ ] 返回 AI 标签和用户修正标签
- [ ] 支持用户修正写入

完成标准：
- [ ] 打开图片可显示所有标签
- [ ] 用户修改标签可持久化

#### P4.3 看图查标签（外部临时）

- [ ] 接收 Share Intent 或 Photo Picker 图片
- [ ] 计算 SHA256 查缓存
- [ ] 缓存命中直接返回
- [ ] 缓存未命中调用 AI 分析
- [ ] 提供"保存入库"选项

完成标准：
- [ ] 外部图片可查询标签
- [ ] 缓存机制减少重复调用

---

### P5. 标签标准化（v2 功能）

#### P5.1 标签别名管理

- [ ] 实现 `addAlias(alias, canonical)` 方法
- [ ] 实现 `getCanonical(tag)` 方法
- [ ] 实现 H5 管理页面 JSBridge 接口

#### P5.2 H5 标签管理页面

- [ ] 创建 `h5/src/views/TagManager.vue`
- [ ] 实现标签列表展示
- [ ] 实现别名添加/删除
- [ ] 实现标签合并功能

完成标准：
- [ ] 可通过 H5 页面管理标签别名
- [ ] Native 调用 JSBridge 同步数据

---

### P6. 以图反查 v1

#### P6.1 pHash 计算

- [ ] 添加图片哈希库依赖或自行实现
- [ ] 实现感知哈希（pHash）计算
- [ ] 存储到 `image_feature` 表

完成标准：
- [ ] 图片 pHash 正确计算
- [ ] 相似图片哈希值接近

#### P6.2 标签召回

- [ ] 提取查询图片的标签
- [ ] 通过标签召回候选图片
- [ ] 限制候选数量（Top 200）

完成标准：
- [ ] 标签召回返回相关候选

#### P6.3 粗排实现

- [ ] 计算标签重合度
- [ ] 计算 pHash 汉明距离
- [ ] 综合排序返回 TopK

完成标准：
- [ ] 以图反查返回 Top 20 相似图片
- [ ] 结果按相似度排序

---

### P7. JSBridge 扩展

新增 JSBridge 方法：

- [ ] `pickImage()` - 调用系统图片选择器
- [ ] `queryImageTags(sha256)` - 查询图片标签
- [ ] `searchImages(query)` - 文本检索
- [ ] `saveUserTags(imageId, tags)` - 用户修正标签
- [ ] `getTagAliases()` - 获取标签别名列表
- [ ] `addTagAlias(alias, canonical)` - 添加标签别名
- [ ] `findSimilarImages(sha256)` - 以图反查

---

### P8. H5 页面扩展

- [ ] 创建搜索页面 `SearchView.vue`
- [ ] 创建图片详情页面 `ImageDetailView.vue`
- [ ] 创建标签管理页面 `TagManagerView.vue`
- [ ] 配置 Vue Router

---

## 4. 交付物清单

### 代码

- [ ] Room 数据库层（Entity + DAO + Database）
- [ ] 图片扫描与预处理模块
- [ ] AI API 调用与结果处理
- [ ] FTS5 搜索实现
- [ ] JSBridge 扩展方法
- [ ] H5 新增页面

### 测试

- [ ] DAO 单元测试
- [ ] 搜索功能集成测试
- [ ] AI Mock 测试

### 文档

- [ ] 更新 README.md（新增功能说明）
- [ ] 更新 CLAUDE.md（新增模块说明）

---

## 5. 一票否决项

- [ ] 数据库迁移失败导致数据丢失
- [ ] AI 调用无超时导致 App 卡死
- [ ] FTS5 索引未同步导致搜索无结果
- [ ] 大量图片扫描导致 OOM

---

## 6. 里程碑完成判定

Phase 2 视为完成当：

- [ ] 图片扫描入库正常工作
- [ ] AI 标注结果正确落库
- [ ] 文本检索返回正确结果
- [ ] 看图查标签功能可用
- [ ] 以图反查返回相似图片
- [ ] H5 标签管理页面可用
