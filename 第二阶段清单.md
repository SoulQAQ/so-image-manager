# 第二阶段清单（Phase 2）

> 目标读者：执行开发任务的 Agent
> 目标版本：v1 + v2 增强版
> 最终目标：实现完整的本地智能图片管理核心功能

## 前置条件

Phase 1 已完成：
- [x] Android 工程骨架（Compose + WebView）
- [x] H5 工程（Vue3 + Vant4）
- [x] JSBridge 通信机制
- [x] Debug/Release APK 打包

## 1. 第二阶段目标

### 核心功能（v1 完整版）

- [x] SQLite 数据库层（Room + Entity + DAO）
- [x] 图片扫描与索引（MediaStore）
- [x] AI 结构化标注与落库
- [x] FTS5 全文检索
- [x] 看图查标签（库内图片）
- [x] 看图查标签（外部临时图片）

### 增强功能（v2）

- [x] 标签标准化工具（alias/canonical）
- [ ] 以图反查 v1（标签召回 + hash 粗排）
- [x] 搜索结果解释与修正闭环

## 2. 环境依赖补充

### Android 新增依赖

```kotlin
// Room
implementation("androidx.room:room-runtime:2.6.1")
implementation("androidx.room:room-ktx:2.6.1")
ksp("androidx.room:room-compiler:2.6.1")

// OkHttp (AI API 调用)
implementation("com.squareup.okhttp3:okhttp:4.12.0")
implementation("com.squareup.okhttp3:logging-interceptor:4.12.0")

// 协程
implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")

// DataStore (配置存储)
implementation("androidx.datastore:datastore-preferences:1.1.1")
```

### 权限补充（AndroidManifest.xml）

```xml
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.INTERNET" />
```

## 3. 任务分解

### P1. 数据基础层 ✅

#### P1.1 Room 数据库配置

- [x] 添加 Room 依赖到 `libs.versions.toml` 和 `app/build.gradle.kts`
- [x] 创建 `AppDatabase.kt` 数据库类
- [x] 配置数据库版本与迁移策略

完成标准：
- [x] `./gradlew assembleDebug` 编译通过
- [x] 数据库类可正常初始化

#### P1.2 Entity 定义

按方案.md第七章定义：

- [x] `ImageEntity.kt` - 图片元数据表
- [x] `ImageAiEntity.kt` - AI 标注结果表
- [x] `TagEntity.kt` - 标签表
- [x] `ImageTagEntity.kt` - 图片-标签关联表
- [x] `TagAliasEntity.kt` - 标签别名表
- [x] `ImageQueryCacheEntity.kt` - 临时查询缓存表
- [x] `ImageFeatureEntity.kt` - 特征表（v2 使用）

完成标准：
- [x] 所有 Entity 定义完成
- [x] 包含正确的索引和外键约束

#### P1.3 DAO 接口

- [x] `ImageDao.kt` - 图片 CRUD
- [x] `ImageAiDao.kt` - AI 结果 CRUD
- [x] `TagDao.kt` - 标签管理
- [x] `ImageTagDao.kt` - 关联关系
- [x] `SearchDao.kt` - FTS5 检索

完成标准：
- [x] 所有基础 CRUD 方法实现
- [x] 包含事务注解的批量操作方法

#### P1.4 FTS5 虚拟表

- [x] 创建 `image_fts` 虚拟表
- [x] 实现插入触发器（自动同步 search_tokens）
- [x] 实现 BM25 排序查询

完成标准：
- [x] FTS5 表创建成功
- [x] 全文检索可返回按相关性排序的结果

---

### P2. 图片接入层 ✅

#### P2.1 MediaStore 扫描服务

- [x] 创建 `ImageScanner.kt` 类
- [x] 实现 `scanAllImages()` 全量扫描
- [x] 实现 `scanNewImages(sinceTimestamp)` 增量扫描
- [x] 处理 Android 13+ 权限适配

完成标准：
- [x] 可扫描设备上所有图片
- [x] 增量扫描返回新增图片列表

#### P2.2 图片预处理

- [x] 创建 `ImagePreprocessor.kt` 类
- [x] 实现 SHA256 计算（使用 MessageDigest）
- [x] 实现图片压缩（目标 < 512KB）
- [x] 实现 EXIF 旋转纠正

完成标准：
- [x] 预处理后的图片可上传 AI API
- [x] SHA256 值正确计算并存储

#### P2.3 入队管理

- [x] 创建 `ImageProcessQueue.kt` 处理队列
- [x] 实现图片入库逻辑
- [x] 实现去重（基于 SHA256）
- [x] 实现后台协程处理

完成标准：
- [x] 新图片自动入队处理
- [x] 队列持久化，App 重启后恢复

---

### P3. AI 分析层 ✅

#### P3.1 HTTP 客户端

- [x] 创建 `AiHttpClient.kt` 单例
- [x] 配置 OkHttp（超时、重试、日志）
- [x] 实现 API Key 管理（从 BuildConfig 或 DataStore）

完成标准：
- [x] HTTP 客户端可正常发起请求
- [x] 错误处理完善

#### P3.2 AI API 调用

- [x] 创建 `AiService.kt` 接口
- [x] 实现 `analyzeImage(imageUri: Uri)` 方法
- [x] 解析 AI 返回的 JSON（按方案.md第六章 Schema）

完成标准：
- [x] AI 调用成功并返回结构化数据
- [x] 错误处理（超时、限流、失败）

#### P3.3 结构化落库

- [x] 创建 `AiResultProcessor.kt` 处理类
- [x] 实现 JSON 解析为 Entity
- [x] 实现标签 canonical 化（查 alias 表）
- [x] 实现多表事务写入

完成标准：
- [x] AI 结果正确写入 image_ai、tag、image_tag 表
- [x] FTS 索引自动更新

#### P3.4 版本追踪

- [x] 记录 `ai_model`、`prompt_version`、`schema_version`
- [x] 实现按版本查询/重跑能力

---

### P4. 搜索与检索层 ✅

#### P4.1 文本检索

- [x] 创建 `SearchService.kt`
- [x] 实现关键词 canonical 化
- [x] 实现 FTS5 MATCH 查询
- [x] 实现 BM25 排序

完成标准：
- [x] 输入关键词可返回相关图片列表
- [x] 支持多关键词 AND 查询

#### P4.2 看图查标签（库内）

- [x] 实现 `getImageTags(imageId)` 方法
- [x] 返回 AI 标签和用户修正标签
- [x] 支持用户修正写入

完成标准：
- [x] 打开图片可显示所有标签
- [x] 用户修改标签可持久化

#### P4.3 看图查标签（外部临时）

- [x] 接收 Share Intent 或 Photo Picker 图片
- [x] 计算 SHA256 查缓存
- [x] 缓存命中直接返回
- [x] 缓存未命中调用 AI 分析
- [x] 提供"保存入库"选项

完成标准：
- [x] 外部图片可查询标签
- [x] 缓存机制减少重复调用

---

### P5. 标签标准化 ✅

#### P5.1 标签别名管理

- [x] 实现 `addAlias(alias, canonical)` 方法
- [x] 实现 `getCanonical(tag)` 方法
- [x] 实现 H5 管理页面 JSBridge 接口

#### P5.2 H5 标签管理页面

- [x] 创建 `h5/src/views/TagManagerView.vue`
- [x] 实现标签列表展示
- [x] 实现别名添加/删除
- [ ] 实现标签合并功能

完成标准：
- [x] 可通过 H5 页面管理标签别名
- [x] Native 调用 JSBridge 同步数据

---

### P6. 以图反查 v1 ⏳

#### P6.1 pHash 计算

- [ ] 添加图片哈希库依赖或自行实现
- [ ] 实现感知哈希（pHash）计算
- [ ] 存储到 `image_feature` 表

完成标准：
- [ ] 图片 pHash 正确计算
- [ ] 相似图片哈希值接近

#### P6.2 标签召回

- [ ] 提取查询图片的标签
- [ ] 通过标签召回候选图片
- [ ] 限制候选数量（Top 200）

完成标准：
- [ ] 标签召回返回相关候选

#### P6.3 粗排实现

- [ ] 计算标签重合度
- [ ] 计算 pHash 汉明距离
- [ ] 综合排序返回 TopK

完成标准：
- [ ] 以图反查返回 Top 20 相似图片
- [ ] 结果按相似度排序

---

### P7. JSBridge 扩展 ✅

新增 JSBridge 方法：

- [ ] `pickImage()` - 调用系统图片选择器
- [x] `getImageTags(imageId)` - 查询图片标签
- [x] `searchImages(query)` - 文本检索
- [x] `saveUserTags(imageId, tags)` - 用户修正标签
- [x] `getTagAliases()` - 获取标签别名列表
- [x] `addTagAlias(alias, canonical)` - 添加标签别名
- [x] `deleteTagAlias(alias)` - 删除标签别名
- [x] `getStatistics()` - 获取统计信息
- [x] `getAllTags()` - 获取所有标签

---

### P8. H5 页面扩展 ✅

- [x] 创建搜索页面 `SearchView.vue`
- [x] 创建图片详情页面 `ImageDetailView.vue`
- [x] 创建标签管理页面 `TagManagerView.vue`
- [x] 创建首页视图 `HomeView.vue`
- [x] 配置 Vue Router

---

## 4. 交付物清单

### 代码

- [x] Room 数据库层（Entity + DAO + Database）
- [x] 图片扫描与预处理模块
- [x] AI API 调用与结果处理
- [x] FTS5 搜索实现
- [x] JSBridge 扩展方法
- [x] H5 新增页面

### 测试

- [ ] DAO 单元测试
- [ ] 搜索功能集成测试
- [ ] AI Mock 测试

### 文档

- [x] 更新 README.md（新增功能说明）
- [x] 更新 CLAUDE.md（新增模块说明）

---

## 5. 一票否决项

- [x] 数据库迁移失败导致数据丢失 — 已验证
- [x] AI 调用无超时导致 App 卡死 — 已配置超时
- [x] FTS5 索引未同步导致搜索无结果 — 已实现同步
- [x] 大量图片扫描导致 OOM — 已实现分批处理

---

## 6. 里程碑完成判定

Phase 2 视为完成当：

- [x] 图片扫描入库正常工作
- [x] AI 标注结果正确落库
- [x] 文本检索返回正确结果
- [x] 看图查标签功能可用
- [ ] 以图反查返回相似图片 — 待实现
- [x] H5 标签管理页面可用

---

## 版本发布

### v0.2.0 (2026-05-12)

已发布到 `apk/image-ai-v0.2.0.apk`

完成：
- P1 数据基础层
- P2 图片接入层
- P3 AI分析层
- P4 搜索检索层
- P5 标签标准化
- P7 JSBridge扩展
- P8 H5页面扩展

待完成：
- P6 以图反查 v1（计划 v0.3.0）
