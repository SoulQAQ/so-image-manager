# SoIM v0.19.0

- 首次使用长期正式证书签名，为后续版本建立稳定的应用身份和覆盖更新基础。
- 首次启动可在申请图库权限前选择旧版迁移备份，恢复图片索引、AI 分析和应用配置。
- 从旧版恢复的供应方不会携带 API Key；缺少凭据的供应方保持禁用，并集中提示重新填写。
- 现有 Debug 版不能直接覆盖安装此版本，请先通过 `v0.18.3` 的“迁移到正式版”完成备份与安装包保存。
